export interface FormData {
  petType: string;
  petName: string;
  petAge: number;
  petBreed: string;
  ownerName: string;
  email: string;
  phoneNumber: string;
  appointmentDate: Date;
  appointmentTime: string;
}

export interface RoadmapStep {
  id: number;
  label: string;
}
